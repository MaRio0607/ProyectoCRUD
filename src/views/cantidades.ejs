<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Nodejs Mysql CRUD</title>
    <!-- NAVIGATION -->
    <link rel="stylesheet" href="https://bootswatch.com/4/lux/bootstrap.min.css">
  </head>
  <body>

    <!-- navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <a class="navbar-brand" href="/">Casa Tlatoani</a>
    </nav>
    <div class="col-md-7">
      <table class="table table-bordered table-hover">
        <thead>
          <tr>
            <td>Cantidad de Actividades</td>

          </tr>
        </thead>
        <tbody>
          <% if (data) { %>
            <% console.log('en EJS',typeof(data[0]['cantidad_actividad(1)'])) %>

              <tr>
                  <td><%= data[0]['cantidad_actividad(1)']%> </td>
              
              </tr>
          <% } %>
        </tbody>
      </table>
    </div>

  </body>
</html>